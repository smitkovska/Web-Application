@model WebApplication10.Models.AddToRoleModel
@{
    ViewBag.Title = "AddUserToRole";
}

<h2>Add user with this mail to a role:</h2>
@using(Html.BeginForm("AddUserRole","Account"))
            {
                <div class="form-group">
                    @Html.LabelFor(z=> z.Email)
                    @Html.TextBoxFor(m=>m.Email,new {@class="form-control"})
                </div>
                <div class="form-group">
                    @Html.LabelFor(z=>z.SelectedRole)
                    @Html.DropDownListFor(z => z.SelectedRole, new SelectList(Model.Roles),new {@class="form-control"})
                </div>
                <button type="submit"class="btn btn-default">Save</button>     
                
                }

